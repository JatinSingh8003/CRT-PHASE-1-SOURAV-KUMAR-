// make a stack of plates, where if the stack gets too high, it starts a new stack

#include<iostream>
using namespace std;

class stack {
    private:
        Node * head;
        int max_size;
        int current_size;
        stack(int x) {
            max_size=x;
            current_size=0;
            head=NULL;
        }
    void push(int x) {
        
    }

         
}